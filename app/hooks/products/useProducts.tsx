import React from 'react';
import {useMutation, useQuery} from '@tanstack/react-query';
import {useProductContext} from '../../apis/products/Products';

export default function useProducts(loadingCallback?: () => void) {
  return {};
}
